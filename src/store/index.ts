// ============================================================
// Zustand Store — Lead Form State (nếu cần global state)
// ============================================================

export {};

// TODO: Implement Zustand store khi cần shared state
// import { create } from 'zustand';
//
// interface AppState {
//   isFormOpen: boolean;
//   setFormOpen: (open: boolean) => void;
// }
//
// export const useAppStore = create<AppState>((set) => ({
//   isFormOpen: false,
//   setFormOpen: (open) => set({ isFormOpen: open }),
// }));
